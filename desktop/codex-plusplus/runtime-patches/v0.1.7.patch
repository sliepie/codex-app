diff --git a/main.js b/main.js
index 5988a09..3e0cb90 100644
--- a/main.js
+++ b/main.js
@@ -2302,6 +2302,7 @@ function normalizeStoreEntry(input) {
   if (!manifest?.id || !manifest.name || !manifest.version) {
     throw new Error(`Store entry for ${repo} is missing manifest fields`);
   }
+  const manifestId = assertSafeTweakId(manifest.id, "store tweak id");
   if (normalizeGitHubRepo(manifest.githubRepo) !== repo) {
     throw new Error(`Store entry ${manifest.id} repo does not match manifest githubRepo`);
   }
@@ -2309,8 +2310,8 @@ function normalizeStoreEntry(input) {
     throw new Error(`Store entry ${manifest.id} must pin a full approved commit SHA`);
   }
   return {
-    id: manifest.id,
-    manifest,
+    id: manifestId,
+    manifest: { ...manifest, id: manifestId },
     repo,
     approvedCommitSha: String(entry.approvedCommitSha),
     approvedAt: typeof entry.approvedAt === "string" ? entry.approvedAt : "",
@@ -2374,6 +2375,7 @@ var SIGNED_CODEX_BACKUP = (0, import_node_path6.join)(userRoot, "backup", "Codex
 var CODEX_PLUSPLUS_VERSION = "0.1.7";
 var CODEX_PLUSPLUS_REPO = "b-nnett/codex-plusplus";
 var TWEAK_STORE_INDEX_URL = process.env.CODEX_PLUSPLUS_STORE_INDEX_URL ?? DEFAULT_TWEAK_STORE_INDEX_URL;
+var tweakUpdateCheckQueue = Promise.resolve();
 var CODEX_WINDOW_SERVICES_KEY = "__codexpp_window_services__";
 (0, import_node_fs6.mkdirSync)(LOG_DIR, { recursive: true });
 (0, import_node_fs6.mkdirSync)(TWEAKS_DIR, { recursive: true });
@@ -2446,10 +2448,35 @@ function cleanOptionalString(value) {
   const trimmed = value.trim();
   return trimmed ? trimmed : void 0;
 }
+function isSafeTweakId(id) {
+  return /^[a-zA-Z0-9._-]+$/.test(id) && id !== "." && id !== "..";
+}
+function assertSafeTweakId(value, label = "tweak id") {
+  const id = String(value ?? "");
+  if (!isSafeTweakId(id)) throw new Error(`bad ${label}`);
+  return id;
+}
 function isPathInside(parent, target) {
   const rel = (0, import_node_path6.relative)((0, import_node_path6.resolve)(parent), (0, import_node_path6.resolve)(target));
   return rel === "" || !!rel && !rel.startsWith("..") && !(0, import_node_path6.isAbsolute)(rel);
 }
+function assertExistingRealPathInside(parent, target, label) {
+  const realParent = (0, import_node_fs6.realpathSync)(parent);
+  const stat4 = (0, import_node_fs6.lstatSync)(target);
+  if (stat4.isSymbolicLink()) throw new Error(`${label} cannot be a symlink`);
+  const realTarget = (0, import_node_fs6.realpathSync)(target);
+  if (!isPathInside(realParent, realTarget)) throw new Error(`${label} outside allowed directory`);
+  return realTarget;
+}
+function assertWritablePathInside(parent, target, label) {
+  const realParent = (0, import_node_fs6.realpathSync)(parent);
+  const realDir = (0, import_node_fs6.realpathSync)((0, import_node_path6.dirname)(target));
+  if (!isPathInside(realParent, realDir)) throw new Error(`${label} outside allowed directory`);
+  if ((0, import_node_fs6.existsSync)(target) && (0, import_node_fs6.lstatSync)(target).isSymbolicLink()) {
+    throw new Error(`${label} cannot be a symlink`);
+  }
+  return target;
+}
 function log(level, ...args) {
   const line = `[${(/* @__PURE__ */ new Date()).toISOString()}] [${level}] ${args.map((a) => typeof a === "string" ? a : JSON.stringify(a)).join(" ")}
 `;
@@ -2625,7 +2652,7 @@ import_electron.app.on("will-quit", () => {
   }
 });
 import_electron.ipcMain.handle("codexpp:list-tweaks", async () => {
-  await Promise.all(tweakState.discovered.map((t) => ensureTweakUpdateCheck(t)));
+  scheduleTweakUpdateChecks(tweakState.discovered);
   const updateChecks = readState().tweakUpdateChecks ?? {};
   return tweakState.discovered.map((t) => ({
     manifest: t.manifest,
@@ -2725,7 +2752,8 @@ import_electron.ipcMain.handle("codexpp:read-tweak-source", (_e, entryPath) => {
   if (!isPathInside(TWEAKS_DIR, resolved)) {
     throw new Error("path outside tweaks dir");
   }
-  return require("node:fs").readFileSync(resolved, "utf8");
+  const realPath = assertExistingRealPathInside(TWEAKS_DIR, resolved, "tweak source");
+  return (0, import_node_fs6.readFileSync)(realPath, "utf8");
 });
 var ASSET_MAX_BYTES = 1024 * 1024;
 var MIME_BY_EXT = {
@@ -2740,22 +2768,23 @@ var MIME_BY_EXT = {
 import_electron.ipcMain.handle(
   "codexpp:read-tweak-asset",
   (_e, tweakDir, relPath) => {
-    const fs = require("node:fs");
     const dir = (0, import_node_path6.resolve)(tweakDir);
     if (!isPathInside(TWEAKS_DIR, dir)) {
       throw new Error("tweakDir outside tweaks dir");
     }
-    const full = (0, import_node_path6.resolve)(dir, relPath);
-    if (!isPathInside(dir, full) || full === dir) {
+    const realDir = assertExistingRealPathInside(TWEAKS_DIR, dir, "tweak dir");
+    const full = (0, import_node_path6.resolve)(realDir, relPath);
+    if (!isPathInside(realDir, full) || full === realDir) {
       throw new Error("path traversal");
     }
-    const stat4 = fs.statSync(full);
+    const realFull = assertExistingRealPathInside(realDir, full, "tweak asset");
+    const stat4 = (0, import_node_fs6.statSync)(realFull);
     if (stat4.size > ASSET_MAX_BYTES) {
       throw new Error(`asset too large (${stat4.size} > ${ASSET_MAX_BYTES})`);
     }
-    const ext = full.slice(full.lastIndexOf(".")).toLowerCase();
+    const ext = realFull.slice(realFull.lastIndexOf(".")).toLowerCase();
     const mime = MIME_BY_EXT[ext] ?? "application/octet-stream";
-    const buf = fs.readFileSync(full);
+    const buf = (0, import_node_fs6.readFileSync)(realFull);
     return `data:${mime};base64,${buf.toString("base64")}`;
   }
 );
@@ -2768,19 +2797,21 @@ import_electron.ipcMain.on("codexpp:preload-log", (_e, level, msg) => {
   }
 });
 import_electron.ipcMain.handle("codexpp:tweak-fs", (_e, op, id, p, c) => {
-  if (!/^[a-zA-Z0-9._-]+$/.test(id)) throw new Error("bad tweak id");
+  id = assertSafeTweakId(id);
   const dir = (0, import_node_path6.join)(userRoot, "tweak-data", id);
   (0, import_node_fs6.mkdirSync)(dir, { recursive: true });
+  if ((0, import_node_fs6.lstatSync)(dir).isSymbolicLink()) throw new Error("tweak data dir cannot be a symlink");
   const full = (0, import_node_path6.resolve)(dir, p);
   if (!isPathInside(dir, full) || full === dir) throw new Error("path traversal");
-  const fs = require("node:fs");
   switch (op) {
     case "read":
-      return fs.readFileSync(full, "utf8");
+      return (0, import_node_fs6.readFileSync)(assertExistingRealPathInside(dir, full, "tweak data"), "utf8");
     case "write":
-      return fs.writeFileSync(full, c ?? "", "utf8");
+      return (0, import_node_fs6.writeFileSync)(assertWritablePathInside(dir, full, "tweak data"), c ?? "", "utf8");
     case "exists":
-      return fs.existsSync(full);
+      if (!(0, import_node_fs6.existsSync)(full)) return false;
+      assertExistingRealPathInside(dir, full, "tweak data");
+      return true;
     case "dataDir":
       return dir;
     default:
@@ -2965,9 +2996,20 @@ async function ensureTweakUpdateCheck(t) {
     updateAvailable: latestVersion ? compareVersions(latestVersion, normalizeVersion(t.manifest.version)) > 0 : false,
     ...next.error ? { error: next.error } : {}
   };
-  state.tweakUpdateChecks ??= {};
-  state.tweakUpdateChecks[id] = check;
-  writeState(state);
+  const nextState = readState();
+  nextState.tweakUpdateChecks ??= {};
+  nextState.tweakUpdateChecks[id] = check;
+  writeState(nextState);
+}
+function scheduleTweakUpdateChecks(tweaks) {
+  const snapshot = [...tweaks];
+  tweakUpdateCheckQueue = tweakUpdateCheckQueue.then(async () => {
+    for (const tweak of snapshot) {
+      await ensureTweakUpdateCheck(tweak);
+    }
+  }).catch((e) => {
+    log("warn", "tweak update check failed:", e);
+  });
 }
 async function fetchLatestRelease(repo, currentVersion, includePrerelease = false) {
   try {
@@ -3095,7 +3137,10 @@ async function installStoreTweak(entry) {
   const work = (0, import_node_fs6.mkdtempSync)((0, import_node_path6.join)((0, import_node_os2.tmpdir)(), "codexpp-store-tweak-"));
   const archive = (0, import_node_path6.join)(work, "source.tar.gz");
   const extractDir = (0, import_node_path6.join)(work, "extract");
-  const target = (0, import_node_path6.join)(TWEAKS_DIR, entry.id);
+  const target = (0, import_node_path6.resolve)(TWEAKS_DIR, entry.id);
+  if (!isPathInside(TWEAKS_DIR, target) || target === (0, import_node_path6.resolve)(TWEAKS_DIR)) {
+    throw new Error("store tweak target outside tweaks dir");
+  }
   const stagedTarget = (0, import_node_path6.join)(work, "staged", entry.id);
   try {
     log("info", `installing store tweak ${entry.id} from ${entry.repo}@${entry.approvedCommitSha}`);
@@ -3227,7 +3272,14 @@ function findTweakRoot(dir) {
 function copyTweakSource(source, target) {
   (0, import_node_fs6.cpSync)(source, target, {
     recursive: true,
-    filter: (src) => !/(^|[/\\])(?:\.git|node_modules)(?:[/\\]|$)/.test(src)
+    filter: (src) => {
+      try {
+        if ((0, import_node_fs6.lstatSync)(src).isSymbolicLink()) return false;
+      } catch {
+        return false;
+      }
+      return !/(^|[/\\])(?:\.git|node_modules)(?:[/\\]|$)/.test(src);
+    }
   });
 }
 async function assertStoreTweakCleanForAutoUpdate(entry, target, work) {
diff --git a/preload/settings-injector.js b/preload/settings-injector.js
index b2a5462..fafa096 100644
--- a/preload/settings-injector.js
+++ b/preload/settings-injector.js
@@ -45,6 +45,8 @@ const state = {
     observer: null,
     fingerprint: null,
     sidebarDumped: false,
+    probeFrame: 0,
+    probeShouldDump: false,
     activePage: null,
     sidebarRoot: null,
     sidebarRestoreHandler: null,
@@ -65,13 +67,36 @@ function safeStringify(v) {
         return String(v);
     }
 }
+function shouldDumpDom() {
+    try {
+        return window.__codexppDebugDomDump === true || window.localStorage?.getItem("codexpp.debugDomDump") === "true";
+    }
+    catch {
+        return false;
+    }
+}
+function runProbe(allowDump = false) {
+    tryInject();
+    if (allowDump && shouldDumpDom())
+        maybeDumpDom();
+}
+function scheduleProbe(allowDump = false) {
+    state.probeShouldDump = state.probeShouldDump || allowDump;
+    if (state.probeFrame)
+        return;
+    state.probeFrame = window.requestAnimationFrame(() => {
+        state.probeFrame = 0;
+        const shouldDump = state.probeShouldDump;
+        state.probeShouldDump = false;
+        runProbe(shouldDump);
+    });
+}
 // ───────────────────────────────────────────────────────────── public API ──
 function startSettingsInjector() {
     if (state.observer)
         return;
     const obs = new MutationObserver(() => {
-        tryInject();
-        maybeDumpDom();
+        scheduleProbe(false);
     });
     obs.observe(document.documentElement, { childList: true, subtree: true });
     state.observer = obs;
@@ -87,21 +112,18 @@ function startSettingsInjector() {
         };
         window.addEventListener(`codexpp-${m}`, onNav);
     }
-    tryInject();
-    maybeDumpDom();
+    runProbe(true);
     let ticks = 0;
     const interval = setInterval(() => {
         ticks++;
-        tryInject();
-        maybeDumpDom();
+        runProbe(false);
         if (ticks > 60)
             clearInterval(interval);
     }, 500);
 }
 function onNav() {
     state.fingerprint = null;
-    tryInject();
-    maybeDumpDom();
+    runProbe(true);
 }
 function onDocumentClick(e) {
     const target = e.target instanceof Element ? e.target : null;
@@ -194,7 +216,6 @@ function tryInject() {
     const itemsGroup = findSidebarItemsGroup();
     if (!itemsGroup) {
         scheduleSettingsSurfaceHidden();
-        plog("sidebar not found");
         return;
     }
     if (state.settingsSurfaceHideTimer) {
@@ -1037,6 +1058,23 @@ function renderReleaseNotesMarkdown(markdown) {
     flushCode();
     return root;
 }
+function openExternal(url) {
+    return electron_1.ipcRenderer.invoke("codexpp:open-external", url).catch((error) => {
+        plog("open external link failed", { url, error: String(error) });
+    });
+}
+function externalLink(text, url, className) {
+    const link = document.createElement("a");
+    link.href = "#";
+    link.className = className;
+    link.textContent = text;
+    link.addEventListener("click", (event) => {
+        event.preventDefault();
+        event.stopPropagation();
+        void openExternal(url);
+    });
+    return link;
+}
 function appendInlineMarkdown(parent, text) {
     const pattern = /(`([^`]+)`|\[([^\]]+)\]\((https?:\/\/[^\s)]+)\)|\*\*([^*]+)\*\*|\*([^*]+)\*)/g;
     let lastIndex = 0;
@@ -1052,13 +1090,7 @@ function appendInlineMarkdown(parent, text) {
             parent.appendChild(code);
         }
         else if (match[3] !== undefined && match[4] !== undefined) {
-            const a = document.createElement("a");
-            a.className = "text-token-text-primary underline underline-offset-2";
-            a.href = match[4];
-            a.target = "_blank";
-            a.rel = "noopener noreferrer";
-            a.textContent = match[3];
-            parent.appendChild(a);
+            parent.appendChild(externalLink(match[3], match[4], "text-token-text-primary underline underline-offset-2"));
         }
         else if (match[5] !== undefined) {
             const strong = document.createElement("strong");
@@ -2038,13 +2070,7 @@ function tweakRow(t, sections, pages) {
     if (m.homepage) {
         if (meta.children.length > 0)
             meta.appendChild(dot());
-        const link = document.createElement("a");
-        link.href = m.homepage;
-        link.target = "_blank";
-        link.rel = "noreferrer";
-        link.className = "inline-flex text-token-text-link-foreground hover:underline";
-        link.textContent = "Homepage";
-        meta.appendChild(link);
+        meta.appendChild(externalLink("Homepage", m.homepage, "inline-flex text-token-text-link-foreground hover:underline"));
     }
     if (meta.children.length > 0)
         stack.appendChild(meta);
@@ -2119,13 +2145,7 @@ function renderAuthor(author) {
     }
     wrap.appendChild(document.createTextNode("by "));
     if (author.url) {
-        const a = document.createElement("a");
-        a.href = author.url;
-        a.target = "_blank";
-        a.rel = "noreferrer";
-        a.className = "inline-flex text-token-text-link-foreground hover:underline";
-        a.textContent = author.name;
-        wrap.appendChild(a);
+        wrap.appendChild(externalLink(author.name, author.url, "inline-flex text-token-text-link-foreground hover:underline"));
     }
     else {
         const span = document.createElement("span");
@@ -2358,12 +2378,17 @@ function switchControl(initial, onChange) {
     btn.addEventListener("click", async (e) => {
         e.preventDefault();
         e.stopPropagation();
-        const next = btn.getAttribute("aria-checked") !== "true";
+        const previous = btn.getAttribute("aria-checked") === "true";
+        const next = !previous;
         apply(next);
         btn.disabled = true;
         try {
             await onChange(next);
         }
+        catch (error) {
+            apply(previous);
+            plog("switch change failed", String(error));
+        }
         finally {
             btn.disabled = false;
         }
@@ -2513,6 +2538,8 @@ function findContentArea() {
     return null;
 }
 function maybeDumpDom() {
+    if (!shouldDumpDom())
+        return;
     try {
         const sidebar = findSidebarItemsGroup();
         if (sidebar && !state.sidebarDumped) {
diff --git a/preload/tweak-host.js b/preload/tweak-host.js
index 08d4271..2a245b8 100644
--- a/preload/tweak-host.js
+++ b/preload/tweak-host.js
@@ -18,6 +18,7 @@ exports.teardownTweakHost = teardownTweakHost;
 const electron_1 = require("electron");
 const settings_injector_1 = require("./settings-injector");
 const react_hook_1 = require("./react-hook");
+const TWEAK_LOAD_TIMEOUT_MS = 5000;
 const loaded = new Map();
 let cachedPaths = null;
 async function startTweakHost() {
@@ -31,6 +32,7 @@ async function startTweakHost() {
     // Stash for the settings injector's empty-state message.
     window.__codexpp_tweaks_dir__ =
         paths.tweaksDir;
+    const loadPromises = [];
     for (const t of tweaks) {
         if (t.manifest.scope === "main")
             continue;
@@ -38,20 +40,27 @@ async function startTweakHost() {
             continue;
         if (!t.enabled)
             continue;
-        try {
-            await loadTweak(t, paths);
-        }
-        catch (e) {
+        loadPromises.push(withTimeout(loadTweak(t, paths), TWEAK_LOAD_TIMEOUT_MS, t.manifest.id).catch((e) => {
             console.error("[codex-plusplus] tweak load failed:", t.manifest.id, e);
             try {
                 electron_1.ipcRenderer.send("codexpp:preload-log", "error", "tweak load failed: " + t.manifest.id + ": " + String(e?.stack ?? e));
             }
             catch { }
-        }
+        }));
     }
+    await Promise.all(loadPromises);
     console.info(`[codex-plusplus] renderer host loaded ${loaded.size} tweak(s):`, [...loaded.keys()].join(", ") || "(none)");
     electron_1.ipcRenderer.send("codexpp:preload-log", "info", `renderer host loaded ${loaded.size} tweak(s): ${[...loaded.keys()].join(", ") || "(none)"}`);
 }
+function withTimeout(promise, timeoutMs, id) {
+    let timer = 0;
+    const timeout = new Promise((_, reject) => {
+        timer = setTimeout(() => reject(new Error(`timeout loading tweak ${id}`)), timeoutMs);
+    });
+    return Promise.race([promise, timeout]).finally(() => {
+        clearTimeout(timer);
+    });
+}
 /**
  * Stop every renderer-scope tweak so a subsequent `startTweakHost()` will
  * re-evaluate fresh source. Module cache isn't relevant since we eval
@@ -147,16 +156,22 @@ function makeRendererApi(manifest, paths) {
                 const existing = document.querySelector(sel);
                 if (existing)
                     return resolve(existing);
-                const deadline = Date.now() + timeoutMs;
+                let settled = false;
+                const finish = (callback) => {
+                    if (settled)
+                        return;
+                    settled = true;
+                    clearTimeout(timer);
+                    obs.disconnect();
+                    callback();
+                };
+                const timer = setTimeout(() => {
+                    finish(() => reject(new Error(`timeout waiting for ${sel}`)));
+                }, timeoutMs);
                 const obs = new MutationObserver(() => {
                     const el = document.querySelector(sel);
                     if (el) {
-                        obs.disconnect();
-                        resolve(el);
-                    }
-                    else if (Date.now() > deadline) {
-                        obs.disconnect();
-                        reject(new Error(`timeout waiting for ${sel}`));
+                        finish(() => resolve(el));
                     }
                 });
                 obs.observe(document.documentElement, { childList: true, subtree: true });
diff --git a/tweak-store.js b/tweak-store.js
index d360426..ba1791b 100644
--- a/tweak-store.js
+++ b/tweak-store.js
@@ -63,6 +63,7 @@ function normalizeStoreEntry(input) {
     if (!manifest?.id || !manifest.name || !manifest.version) {
         throw new Error(`Store entry for ${repo} is missing manifest fields`);
     }
+    const manifestId = assertSafeTweakId(manifest.id, "store tweak id");
     if (normalizeGitHubRepo(manifest.githubRepo) !== repo) {
         throw new Error(`Store entry ${manifest.id} repo does not match manifest githubRepo`);
     }
@@ -70,8 +71,8 @@ function normalizeStoreEntry(input) {
         throw new Error(`Store entry ${manifest.id} must pin a full approved commit SHA`);
     }
     return {
-        id: manifest.id,
-        manifest,
+        id: manifestId,
+        manifest: { ...manifest, id: manifestId },
         repo,
         approvedCommitSha: String(entry.approvedCommitSha),
         approvedAt: typeof entry.approvedAt === "string" ? entry.approvedAt : "",
@@ -81,6 +82,12 @@ function normalizeStoreEntry(input) {
         reviewUrl: optionalGithubUrl(entry.reviewUrl),
     };
 }
+function assertSafeTweakId(value, label = "tweak id") {
+    const id = String(value ?? "");
+    if (!/^[a-zA-Z0-9._-]+$/.test(id) || id === "." || id === "..")
+        throw new Error(`bad ${label}`);
+    return id;
+}
 function storeArchiveUrl(entry) {
     if (!isFullCommitSha(entry.approvedCommitSha)) {
         throw new Error(`Store entry ${entry.id} is not pinned to a full commit SHA`);
