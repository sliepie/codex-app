interface:
  display_name: "Codex App Tweak Maintenance"
  short_description: "Trace, patch, and sync Codex++ UI tweaks"
  default_prompt: "Use $codex-app-tweak-maintenance to trace an upstream UI change, patch the bundled tweak, and sync the installed copy."
